import { Layout } from '@/components/layouts';
import { AllProjects } from '@/components/organisms';

const ProjectsPage = () => {
  return (
    <Layout>
      <AllProjects />
    </Layout>
  );
};

export default ProjectsPage;
