import { Layout } from '@/components/layouts';
import { Hero, ProjectGrid } from '@/components/organisms';

const Home = () => {
  return (
    <Layout>
      <Hero />
      <ProjectGrid />
    </Layout>
  );
};

export default Home;
